---
import Carousel from './UI/Carousel.astro'
import MobileProjectCard from './UI/MobileProjectCard.astro'
import DesktopProjectCard from './UI/DesktopProjectCard.astro'
import LandingProjectCard from './UI/LandingProjectCard.astro'

import { Experience } from '../../scripts/ExperienceBase.js'
import { PCExperience, LandingExperience } from '../../scripts/ExperienceBase.js'
---

<header class="w-full flex flex-col gap-2 text-center mb-12" style="font-family: outfit; color:white;">
    <h1 class="text-3xl font-semibold text-amber-300" style="opacity: 1; transform: none;">Projectos</h1>
    <p class="opacity-70">
        Aquí hay algunos proyectos en los que he trabajado recientemente.
    </p>
</header>

<section class="w-full flex flex-col gap-16">
    
    <!-- Carrusel de Apps Móviles -->
    <Carousel 
        id="mobile-carousel"
        title="📱 Mobile Applications"
        description="Native mobile apps built with .NET MAUI and modern technologies"
        projects={Experience}
        type="mobile"
    >
        {Experience.map(project => (
            <MobileProjectCard project={project} />
        ))}
    </Carousel>

    
    

   





    <!-- Carrusel de Landing Pages -->
    <Carousel 
        id="landing-carousel"
        title="🌐 Landing Pages"
        description="Modern and responsive web experiences"
        projects={LandingExperience}
        type="desktop"
    >
        {LandingExperience.map(project => (
            <LandingProjectCard project={project} />
        ))}
    </Carousel>

</section>  